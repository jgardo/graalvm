package dev.jgardo.graalvm.playground;

import io.quarkus.test.junit.NativeImageTest;

@NativeImageTest
public class NativeGenerateControllerIT extends GenerateControllerTest {

    // Execute the same tests but in native mode.
}
